Ensure that all below downloaded:
pip install flask
pip install groq


To run, type
python app.py
Then open in browser

For static folder, put all styles (css files) there 

To run on phone, go to render.com
Then "My Project" > "BC3415_2025S2_Wednesday" > "Manual Deploy" > "Deploy latest commit"
Wait until "Live" with a green tick is seen, then can use the link above to run on phone